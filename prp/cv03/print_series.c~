#include <stdio.h>

int main()
{
	int n = 0;
	scanf("%d", &n);
	
	for (int i = 1; i < n+1; i++)
	{
		if (i%2 == 0)
		{
			printf("%d\n", -1);	
		}
		else
		{
			printf("%d\n", 1);
		}
	}
	
	return 0;
}
